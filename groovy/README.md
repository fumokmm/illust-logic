# How to run on Windows

  \> cd illust-logic\groovy  
  \> groovy -cp .\src .\src\illustlogic data99.yml

# How to run on Linux

  $ cd illust-logic/groovy  
  $ groovy -cp src src/illustlogic data99.yml
